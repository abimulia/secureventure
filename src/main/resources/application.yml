spring:
  application:
    name: secureventure
  sql:
    init:
      mode: always
      continue-on-error: true
logging:
  level:
    ROOT: INFO
    org.hibernate: INFO
    com.abimulia.secureventure: DEBUG
