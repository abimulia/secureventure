spring:
  application:
    name: secureventure
  sql:
    init:
      mode: always
      continue-on-error: true
logging:
  level:
    ROOT: ERROR
    org.hibernate: DEBUG
    com.abimulia.secureventure: DEBUG
