spring:
  application:
    name: secureventure
  sql:
    init:
      mode: always
      continue-on-error: true
logging:
  level:
    ROOT: DEBUG
    org.hibernate: DEBUG
    com.abimulia.secureventure: DEBUG
