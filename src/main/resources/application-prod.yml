spring:
  application:
    name: secureventure
  sql:
    init:
      mode: always
      continue-on-error: true
logging:
  level:
    ROOT: ERROR
    org.hibernate: ERROR
    com.abimulia.secureventure: INFO
